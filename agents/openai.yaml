interface:
  display_name: "Public Good Editorial Flipbook"
  short_description: "Create a presentation from this saved template"
  icon_large: "./assets/preview.png"
  default_prompt: "Use $artifact-template-public-good-editorial-flipbook to create a presentation with this template."
policy:
  allow_implicit_invocation: true
